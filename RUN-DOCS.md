# How to run the docs live
Prerequisites:
> * Node with npm and yarn
>   * Yarn should be installed globally
> * A current version of python3 and pip
>   * Install the required modules in `requirements.txt`